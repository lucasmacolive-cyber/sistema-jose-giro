const fs = require('fs'); const b64 = fs.readFileSync('artifacts/escola/public/images/brasao.png', 'base64'); const path = 'artifacts/escola/src/components/CabecalhoTimbrado.tsx'; let content = fs.readFileSync(path, 'utf8'); const rx = new RegExp('export const BRASAO_URL = \\'data:image\\\\/png;base64,[^\\']+\\';'.replace(/'/g, '"')); content = content.replace(rx, 'export const BRASAO_URL = "data:image/png;base64,' + b64 + '";'); fs.writeFileSync(path, content);
